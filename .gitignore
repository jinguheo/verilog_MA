__pycache__/
*.pyc
.pytest_cache/

node_modules/
my_dashboard/dist/

# Generated verification artifacts and local operational files
logs/
*.lnk
sidebar-*.png
samples/**/obj_*/
samples/**/obj_dir/
samples/**/tmp_*/
samples/**/formal/*/
samples/**/*.out
samples/**/uvm/uvm-regression*
.local-cache/

# OpenLane run output (GDS/LEF/netlist/logs per step) - reproducible via
# tools/wsl/80_run_openlane.sh / 87_run_chan_top.sh, and large (multi-GB across
# runs). config.json and constraints/*.sdc alongside these ARE tracked - only
# the generated runs/ trees and stray render output are excluded.
samples/**/asic/*/runs/
samples/**/asic/*/layout/
samples/**/slpp_all/

# Diagnostic script output from tools/wsl/*.sh - reproducible, not source.
tools/wsl/logs/

oss-cad-suite/
third_party/installers/
third_party/uvm-core/

# Installed analog tool environments, third-party sources and generated runs.
analog/.venv-cace/
analog/.venv-openfasoc/
analog/third_party/
analog/runs/
analog/jobs/
analog/build/
docs/session_notes/
.claude/settings.local.json

# SkyWater PDK install (volare), ~2GB - reinstallable, not source. See
# NEXT_SESSION.md's 2026-09-14 entry.
/pdk/

# Stray Python venv accidentally created at the repo root (pyvenv.cfg's own
# "command" line shows `python3 -m venv /mnt/d/MyWork/Veriolg_MA` - it should
# have been created in a subdirectory). Not source, do not commit.
/bin/
/lib/
/pyvenv.cfg
