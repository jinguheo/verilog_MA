interface Props { tabs: Array<{id:string; label:string; count?:number}>; current:string; onSelect:(id:string)=>void }
export default function Sidebar({tabs,current,onSelect}: Props){return <aside className="sidebar"><div className="logo"><span>V</span><div><b>Veriolg MA</b><small>Knowledge Console</small></div></div><div className="side-caption">WORKSPACE</div><div className="workspace"><i/> <div><b>verilog knowledge</b><small>read-only connection</small></div></div><nav>{tabs.map(tab=><button key={tab.id} className={current===tab.id?'active':''} onClick={()=>onSelect(tab.id)}><span>{tab.label}</span>{tab.count!==undefined&&<em>{tab.count}</em>}</button>)}</nav><div className="sidebar-bottom">● knowledge scan online</div></aside>}
